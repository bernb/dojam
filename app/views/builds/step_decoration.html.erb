<%= render 'layouts/title', title_name: "add new object" %>
<%= render 'layouts/new_entry_progress_bar', state: @step %>
<div class="row">
  <div class="col-md-2">
    <%= simple_form_for @museum_object, url: wizard_path do |f| %>
      <div><strong> technique </strong></div>
      <%= f.association :termlist_decoration, collection: @decorations, label: false, as: :collection_select, include_blank: false %>
      <div><strong> color </strong></div>
      <%= f.association :termlist_decoration_color, collection: @decoration_colors, label: false, as: :collection_select, include_blank: false  %>
      <%= f.submit "next step", method: :get, class: "btn btn-primary" %></p>
      <%= link_to "back", previous_wizard_path, class: "btn" %> 
      <%= link_to "finish", wizard_path(:step_confirm), class: "btn" %>
    <% end %>
  </div>
</div>

