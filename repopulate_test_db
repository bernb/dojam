Inside db container:
touch dump.sql
chown postgres:postgres dump.sql
su postgres
pg_dump -a -O DOJAM_DB > dump.sql
# -a: --data-only
# -c: --clean (drop before)
psql -U TEST_dojam -a -c -d TEST_DB -f dump.sql
